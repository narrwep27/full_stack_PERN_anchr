export default () => {
  interval = setInterval(()=> {
    if  
  })
  // let interval = null;
  // if (start) {
  //   interval = setInterval(() => {
  //     if (time > 0) {
  //       setTime((previousTime) => previousTime - 10);
  //     } else {
  //       logSession();
  //       setStart(false);
  //     }
  //   }, 10);
  // } else {
  //   setStart(false);
  //   clearInterval(interval);
  // }
  // return () => {
  //   clearInterval(interval);
  // };
};
